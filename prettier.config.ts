import { type Config } from 'prettier';

const config: Config = {
  singleQuote: true,
  semi: true,
  tabWidth: 2,
  printWidth: 80,
  trailingComma: 'all',
  bracketSameLine: false,
};

export default config;
